#include <stdio.h>

int main() {
    int num1, num2, result;

    scanf("%d%d", &num1, &num2);


    if (num1 == num2) {
        printf("Os numeros sao iguais");
    } else {
        printf("Os numeros nao sao iguais");
    }
    return 0;
}
